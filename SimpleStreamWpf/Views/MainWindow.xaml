<Window x:Class="SimpleStreamWpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleStreamWpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded" Closing="Window_Closing">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="400"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="30"/>
    </Grid.RowDefinitions>
    <TextBox  Margin="5" TextWrapping="Wrap" Text="{Binding Path=Log}" />
    <Button Content="Continue" HorizontalAlignment="Left" Margin="5" Grid.Row="1" Width="75" Command="{Binding ContinueCommand}"/>
    <WebBrowser Name="browser" 
                Grid.Column="1"
                VerticalAlignment="Top"
                Height="219"
                Margin="5"/>
    <Button Content="Refresh" HorizontalAlignment="Left" Margin="5" Grid.Row="1" Width="75" Command="{Binding RefreshCommand}" Grid.Column="1"/>
    <Button Content="Open in browser" HorizontalAlignment="Right" Margin="5" Grid.Row="1" Width="100" Command="{Binding OpenInBrowser}" Grid.Column="1"/>
  </Grid>
</Window>
